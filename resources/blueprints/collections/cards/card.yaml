sections:
  main:
    display: Hauptteil
    fields:
      -
        import: card
        config:
          title:
            required: true
  sidebar:
    display: Sidebar
    fields:
      -
        handle: slug
        field:
          type: slug
          localizable: true
title: Karten
